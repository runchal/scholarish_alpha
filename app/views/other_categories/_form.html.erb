	<ul>
		<% @other_category.errors.full_messages.each do |message| %>
			<li><%= message %>
		<% end %>
	</ul>

	<%= form.label :other_category_type, "What's the type of page you want to make?" %><br>
	<%= form.text_area :other_category_type, placeholder:"Book or Title or Publications" %>
	<br>
	<br>
	<%= form.submit %>