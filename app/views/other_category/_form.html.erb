	<ul>
		<% @other_category.errors.full_messages.each do |message| %>
			<li><%= message %>
		<% end %>
	</ul>

	<%= form.label :type, "What's the type of page you want to make?" %><br>
	<%= form.text_area :type, placeholder:"Book or Title or Publications" %>
	<br>
	<br>
	<%= form.submit %>